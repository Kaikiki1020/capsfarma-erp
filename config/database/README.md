# Database Config

Esta pasta centraliza configuracoes de banco consumidas pela aplicacao.

Arquivos atuais:

- `supabase.browser.js`: configuracao usada pelo frontend no navegador

Compatibilidade:

- `frontend/supabase/config.js` continua existindo como link simbolico para este arquivo
